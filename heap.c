#include "constants.h"
#include <stdlib.h>

typedef struct heap {
    // some metadata to mimic
} heap_t;

typedef struct shadow {
    // some metadata to mimic
} shadow_t;

heap_t heaps[NUM_HEAPS];
shadow_t shadows[NUM_HEAPS];

// cost, no actual action
void update_ver_malloc() {

}

// cost, no actual action
void update_ver_separation_malloc() {
    
}